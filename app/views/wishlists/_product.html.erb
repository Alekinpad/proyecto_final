<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Photo</th>
      <th>Price</th>
      <th>Stock</th>
      <th>Store</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <td><%= product.photo %></td>
        <td><%= product.price %></td>
        <td><%= product.stock %></td>
        <td><%= link_to product.store, product.store %></td>
        <td><%= link_to 'Show', [product.store, product] %></td>
        <td><%#= link_to 'Sacar de wishlist', user_wishlist_remove_from_wishlist_path(@user, @wishlist, product),  method: :post %></td>
      </tr>
    <% end %>
  </tbody>
</table>